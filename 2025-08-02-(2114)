class Solution {
    public int mostWordsFound(String[] s) {
        int max=0;
        for(int i=0;i<s.length;i++){
            int count=s[i].split(" ").length;
            if(count>max){
                max=count;
            }
        }
        return max;
    }
}
