class Solution {
    public int mostWordsFound(String[] s) {
        int max=0;
        for(int i=0;i<s.length;i++){
            String arr[]=s[i].split(" ");
            int n=arr.length;
            if(n>max){
                max=n;
            }
        }
        return max;
    }
}
